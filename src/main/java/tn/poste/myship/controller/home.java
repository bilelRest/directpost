/*
 * Ancien controller `home.java` neutralisé.
 * Remplacé par `HomeController.java` (classe publique correcte et conforme aux conventions Java).
 * Gardé ici pour historique ; ne contient plus de code exécutable.
 */

package tn.poste.myship.controller;

public class home {
    // class intentionally left blank
}
